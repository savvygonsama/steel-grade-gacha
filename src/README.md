# src — 앱을 만드는 재료

저장소 뿌리의 `index.html`(약 2.9MB)은 **완성품**입니다. 그 안에 그림 57장이
base64로 박혀 있어 직접 손대기 어렵습니다. 고칠 때는 이 폴더의 재료를 고치고
다시 빌드하세요.

```
app_tpl.html    앱의 뼈대. 캐릭터 명부, 퀴즈, 고객 요구, 확률, 화면 코드가 모두 여기.
                그림 자리는 __P1__ __S13__ __H4__ __I9__ 같은 빈칸으로 비워 둔다.
img/            캐릭터 그림 (480x720 JPEG)
itemimg/        보증 항목 아이콘 (240x240 투명 PNG)
pwa_head.txt    index.html 맨 앞에 붙는 meta·link
pwa_body.txt    index.html 맨 뒤에 붙는 설치 배너와 서비스 워커 등록
build.sh        위를 합쳐 ../index.html 을 만든다
```

## 고치는 순서

```bash
# 1) app_tpl.html 또는 img/ 를 고친다
# 2) 다시 빌드
bash src/build.sh
# 3) 설치된 앱이 갱신되도록 sw.js 의 CACHE 버전을 올린다
# 4) 커밋
```

## 규칙 하나

**완성품 `index.html`을 직접 고치지 마세요.** 다음 빌드 때 덮어써집니다.
2026년 9월에 이 규칙이 없어서 두 갈래로 갈라졌고, 되맞추는 데 품이 들었습니다.
고칠 것이 있으면 언제나 `src/` 쪽입니다.
